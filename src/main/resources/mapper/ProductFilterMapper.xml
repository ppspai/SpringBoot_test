<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="baskin.back.Mapper.ProductFilterMapper">

    <select id = "findProductByFilter" parameterType="hashmap" resultType = "baskin.back.DTO.ProductFilterDTO">
        SELECT
            product.id,
            product.name,
            product.kcal,
            product.salt,
            product.sugar,
            product.fat,
            product.protein,
            product.caffeine,
            allergy.allergy_name
        FROM
            product
        LEFT JOIN (SELECT
                        product.id AS id,
                        array_agg(allergy.allergy_name) AS allergy_name
                    FROM
                        product
                    LEFT JOIN productallergy ON product.id = productallergy.product_id
                    LEFT JOIN allergy ON productallergy.allergy_id = allergy.id
                    GROUP BY product.id) as allergy ON product.id = allergy.id

        <where>
            <if test = "name != null">
                <bind name="names" value="'%'+name+'%'"/>
                AND product.name like #{names}
            </if>
        </where>
        <if test="column!= null and order != null">
            ORDER BY ${column} ${order}
        </if>
    </select>
</mapper>